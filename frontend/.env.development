# En desarrollo (`npm run dev`) el frontend corre en :3001 y el backend en
# :8000, así que apuntamos las llamadas al backend. En producción la app se
# sirve desde el mismo origen del backend y se usa la ruta relativa por defecto.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/promo
